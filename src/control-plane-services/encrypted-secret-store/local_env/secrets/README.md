# Secrets

Disclaimer:

* All secrets in [secrets.json](secrets.json) are generated solely for local testing.
* None of the values are used in deployments or considered sensitive.
* If the application expects specific keys in a certain format (e.g. base64-encoded JWKS), values are generated accordingly. Otherwise, placeholder values are used.

# Agent Helper Scripts

This directory is for small helper scripts that agents may reuse across tasks.

Prefer the repo-level `scripts/` directory for supported project tooling. Use this directory for agent workflow helpers that are documented in `.memory/tools.md`.

# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# GENERATED by internal/manifests -- do not edit.
# Regenerate with: go generate ./internal/manifests/...
#
# Restore placeholder for a criu-v2 (in-namespace) checkpoint of nim-llama-8b.
#
# Dumb reaper: same image as the source (CRIU's path-based file checks resolve
# against an identical rootfs), bash pid1 reaps orphans, no restore-entrypoint
# and no hostPID -- the pod keeps its own fresh pid namespace, which is where
# the in-namespace CRIU restores the dumped session. The agent drives
# everything on POST /v1/restore. See internal/agent/restore_v2.go.
apiVersion: v1
kind: Pod
metadata:
  name: nim-llama-8b-restored
  namespace: nvsnap-system
  labels:
    app: nim-llama-8b-restored
    nvsnap.io/demo: "true"
spec:
  automountServiceAccountToken: false
  # IMPORTANT: must run on the same node as the source pod's checkpoint.
  # test-e2e.sh substitutes __NODE_NAME__ from the source pod's status.
  nodeName: __NODE_NAME__

  imagePullSecrets:
    - name: nvsnap-pull-secret
    - name: nim-pull-secret

  containers:
    - name: restore
      image: nvcr.io/nim/meta/llama-3.1-8b-instruct:1.8.5
      imagePullPolicy: IfNotPresent
      command: ["/bin/bash", "-lc"]
      args:
        - |
          set -e
          # NOTE: this pod does NOT reserve its own pid range. Writing
          # /proc/sys/kernel/ns_last_pid from a container fails with EPERM
          # (/proc/sys is mounted read-only into the pod) even when privileged,
          # so the attempt that used to live here never did anything. The agent
          # does it from the host instead, before invoking CRIU restore --
          # see reservePlaceholderPIDs in internal/agent/restore_v2.go.
          # Restored workload stdio is a plain-file fd on /tmp/nim.out (the
          # source manifest's setsid convention); surface it via kubelet.
          #
          # Do NOT pre-create this file. The rootfs-diff replay delivers it
          # with the exact contents and size CRIU recorded, and CRIU verifies
          # that size before reopening the fd. An empty placeholder both fails
          # that check and blocks the replay that would have fixed it:
          #   tar: ./tmp/nim.out: Cannot open: Permission denied
          #   Error (criu/files-reg.c:2184): File tmp/nim.out has bad size 0
          #                                  (expect 17058)
          #   Error (criu/files.c:1322): Unable to open fd=1 id=0x6
          # touch creates it owned by the image's unprivileged user, and the
          # replay's tar cannot overwrite that. Leaving the path absent lets
          # tar create it cleanly. tail -F already waits for a missing file.
          tail -F /tmp/nim.out &
          while true; do sleep 30; done
      env:
        # CHECKPOINT_ID intentionally in block style -- test-e2e.sh's sed
        # substitution advances to the NEXT line.
        - name: CHECKPOINT_ID
          value: "__CHECKPOINT_ID__"
      readinessProbe:
        httpGet:
          path: /v1/health/ready
          port: 8000
        initialDelaySeconds: 5
        periodSeconds: 5
        timeoutSeconds: 5
        failureThreshold: 180
      securityContext:
        privileged: true
      resources:
        limits:
          nvidia.com/gpu: "1"
        requests:
          nvidia.com/gpu: "1"
      volumeMounts:
        - { name: checkpoints, mountPath: /checkpoints }
        - { name: dev-shm, mountPath: /dev/shm }

  volumes:
    - name: checkpoints
      hostPath:
        path: /var/lib/containerd/nvsnap-checkpoints
        type: Directory
    - name: dev-shm
      emptyDir:
        medium: Memory
        sizeLimit: 16Gi

  restartPolicy: Never

# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

commonLabels:
  nvcf.nvidia.com/observability-target: "true"

# The parent stack derives these monitor-group defaults from
# observability.profile. Explicit user values override those profile defaults.
controlPlane:
  enabled: false
  interval: 30s
  path: /metrics
  port: metrics
  # Default namespace the services run in (ServiceMonitor namespaceSelector).
  # Per-service `namespaces` overrides this to keep each monitor scoped tightly.
  namespaces:
    - nvcf
  services:
    - name: state-metrics
      enabled: true
      selector:
        app.kubernetes.io/instance: state-metrics
        app.kubernetes.io/name: helm-nvcf-state-metrics
    - name: grpc-proxy
      enabled: true
      selector:
        app.kubernetes.io/instance: grpc-proxy
    - name: llm-api-gateway
      enabled: true
      selector:
        app.kubernetes.io/instance: llm-api-gateway
    - name: invocation-service
      enabled: true
      selector:
        app.kubernetes.io/instance: invocation-service

computePlane:
  enabled: false
  interval: 30s
  path: /metrics
  services:
    - name: nvca
      enabled: true
      namespaces:
        - nvca-system
      port: nvca
      selector:
        app.kubernetes.io/name: nvca
      labels: {}

  dcgm:
    enabled: true
    namespace: ""
    namespaceSelector:
      any: true
    selector:
      matchLabels:
        nvca.nvcf.nvidia.io/dcgm-metrics-present: "true"
    interval: 30s
    path: /metrics
    # NVCA uses this name for the injected DCGM metrics port.
    port: dcgm-metrics
    labels: {}

  worker:
    enabled: true
    namespace: ""
    namespaceSelector:
      any: true
    selector:
      matchExpressions:
        - key: icms-request-id
          operator: Exists
    interval: 30s
    path: /metrics
    port: metrics
    labels: {}
